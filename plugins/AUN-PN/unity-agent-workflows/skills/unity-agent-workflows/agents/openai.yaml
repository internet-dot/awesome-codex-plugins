interface:
  display_name: "Unity Agent Workflows"
  short_description: "Unity routing, runtime proof, state guards, and validation"
  default_prompt: "Use $unity-agent-workflows to route Unity 2D work, lock multi-agent scope, and require runtime numeric proof for visible-output/state failures."
policy:
  allow_implicit_invocation: true
